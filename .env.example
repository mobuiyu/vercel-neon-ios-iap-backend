# Neon Postgres
DATABASE_URL="postgres://USER:PASSWORD@HOST/DB?sslmode=require"

# Your iOS app bundle id
APPLE_BUNDLE_ID="com.yourcompany.yourapp"

# App Store Connect API (for App Store Server API v2)
APPLE_ISSUER_ID=""
APPLE_KEY_ID=""
# Paste the .p8 PRIVATE KEY CONTENT (INCLUDING headers), or paste raw key and the code will wrap it.
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
...
-----END PRIVATE KEY-----"

# Optional default env if you call /verify with only transactionId
APPLE_ENV="Production"  # or Sandbox

# Optional: you may set a secret for your own reverse-proxy before calling notifications endpoint.
IAP_WEBHOOK_SHARED_SECRET=""
